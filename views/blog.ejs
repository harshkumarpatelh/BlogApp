<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/head") %>
    <title>Blog</title>
</head>
<body>
   
    <%- include("./partials/nav") %>

    <div class="container mt-4">
        <div><%= blog.title %></div>
        <img src = "<%= blog.coverImageURL %>"  width="400px"/>
        <p class="para mt-3"> <%= blog.body %> </p>
    </div>

    <div class="container mt-4">
        <img src="<%= blog.createdBy.profileImage %>" width="100px" alt="not found"/><%= blog.createdBy.fullName %>
    </div>
    <%- include("./partials/script") %>
</body>
</html>